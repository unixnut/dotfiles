export PYTHONPATH=$HOME/bin

# ~/.local/bin is for "pip install --user"
PATH="$HOME/.local/bin:$PATH"

# Prevent <virtualenv>/bin/activate from altering the prompt
export VIRTUAL_ENV_DISABLE_PROMPT=y
